# 단어방 — 선생님·학생 계정 UX/UI QA 리포트 (2026-09-23)

대상: 테스트학원 연동 계정 — 선생님1 `tester.t1@` · 학생01 `tester.s01@` (비밀번호 `test1234`), `next start` 프로덕션 빌드, http://localhost:3000.
방법: Claude 내장 브라우저로 실제 로그인해 화면마다 (1) 눈으로 보기, (2) 화면 텍스트·DOM 검사(가로 넘침 · 글자 잘림 · 24px 미만 터치 대상 · 표 가로 스크롤 · 콘솔 오류), (3) 휴대폰 폭(375×812)·데스크톱(1366×768) 재검사. 시각 확인은 패널 폭(~548px, 상단 탭 레이아웃) 기준이라 **데스크톱 사이드바 레이아웃은 DOM 검사 위주**임.
우선순위: **P1** 막히거나 틀린 것 · **P2** 헷갈리거나 불편한 것 · **P3** 다듬기.

---

## 0. 한눈에 — 먼저 고칠 10가지

| # | P | 어디 | 무엇 | 어떻게 |
|---|---|---|---|---|
| 1 | **P1** | `/login` · `/login/email` | **로그인된 상태에서 열면 500** (Application error, digest 389860294). 서버 컴포넌트 렌더 중 `landingAfterLogin()` 이 `cookies().set()` 을 호출 → Next 가 던짐. 자리가 1개인 모든 계정(대부분의 선생님·학생)이 해당 | `landingAfterLogin` 에서 쿠키 설정을 빼고 경로만 돌려주거나, 페이지에서는 `getCurrentUser` 만으로 `/app`·`/learn`·`/switch` 로 redirect. 쿠키 설정은 로그인 액션·콜백 라우트에서만 |
| 2 | **P1** | `/app/students` | 반 칩 숫자 = **반 전체 인원**, 목록 = **내 담당 학생** → 선생님1에게 "테스트 B반 5" 인데 누르면 0명. 빈 상태 문구는 "반 코드를 알려주세요"라 담당이 아닌 반에서 오해 | 칩 숫자를 담당 학생 수로(출제 화면과 같은 규칙), 0명 반은 흐리게. 빈 상태는 "이 반에는 내 담당 학생이 없어요" |
| 3 | **P1** | 학생 응시 러너 | 응시 시작을 누르면 **준비 화면 없이 바로 7초 타이머**가 돈다. 답을 고르면 즉시 다음 단어로 넘어가 **잘못 누르면 되돌릴 수 없음** | 러너 첫 화면에 "20문항 · 단어당 7초 · 한 번 고르면 넘어가요 → [시작]" 준비 단계(3-2-1) 추가. 보기 탭 후 200ms 안 다른 보기를 누르면 바꿀 수 있게 하거나, 마지막 보기 확정 전 짧은 확인 |
| 4 | **P2** | 휴대폰 상단 탭(선생님) | 6개 메뉴 + SIGN OUT 이 375px 에서 447px → **재시험 일부·SIGN OUT 이 화면 밖**, 스크롤 힌트 없음 | 휴대폰은 하단 탭 바(오늘·학생·단어장·시험·성적·재시험) 또는 5개 + "더보기". SIGN OUT 은 계정 전환(⇄) 메뉴 안으로 |
| 5 | **P2** | 표 6곳 | 학생 목록·단어 목록(831px)·시험 목록(1148px)·시험 상세 학생 표·응시 대상·성적 두 표가 휴대폰에서 2~3배 가로 스크롤. 줄 작업(수정·출제 제외·삭제·대상에서 제외)이 화면 밖 | 휴대폰은 카드형 목록(이름 + 핵심 2개 + 상태 배지), 줄 작업은 ⋯ 메뉴. 데스크톱은 유지 |
| 6 | **P2** | `/app/results` | 한 페이지에 모듈 8개, 휴대폰 3,889px · 데스크톱 3,074px. 데스크톱 위젯 보드는 오른쪽 90px 이 비고 2행 가운데(780–971px)에 빈 칸 | "요약 / 학생별 / 시험별" 3개 탭으로 나누기. 위젯 보드는 12열을 꽉 채우도록 span 조정(빈 셀 금지) |
| 7 | **P2** | `/app/retakes` | 14줄 모두 세그먼트 + 날짜 + 강조 버튼 + 취소 → 휴대폰 3,567px, 강조색 버튼 42개. "취소"가 뭘 취소하는지 불명, 확인 없음. 8주 전 항목이 계속 남음 | 학생별로 접기(학생05 ×7), 줄은 "오답만 5 · 9/26까지 [출제]" 한 줄 + 펼치면 조정. 취소 → "재시험 없이 종료" + 확인. N주 지난 항목은 자동 정리 제안 |
| 8 | **P2** | `/app` 오늘 | 숫자가 서로 안 맞아 보임: `38/50 · 76% · 미응시 2`, `DUE SOON 0/5`, `14건 내기` vs `출제 전 14명`(학생은 5명). DUE SOON 에 "경과 37일" 지난 시험이 "마감 임박 순"으로 섞임. 카드 바닥 점 7개(요일 막대, 전부 0)는 장식으로 보임 | 라벨을 수치에 맞게: "완료 38 · 남음 12 · 기한 지남 2", "0/5" 제거, 건/명 통일, 지난 시험은 "기한 지남 2" 별도 줄, 막대는 값이 없으면 숨기거나 "이번 주 채점 0" 라벨 |
| 9 | **P2** | 시험 이름 | 같은 단어장·같은 DAY 로 두 번 내면 **이름이 완전히 같음** (`… DAY 1 (9/23)` ×2), 하나는 `0/0`(대상 없음) | 자동 이름에 차수 붙이기 `(9/23 · 2차)`, 대상 0명 시험은 목록에서 "대상 없음" 배지 + 상세로 유도 |
| 10 | **P2** | 시험 목록·상세 | 기본 필터 "진행 중"에 8월에 10/10 끝난 시험이 함께 나옴(상태 열이 전부 "발행"). 상세 `h1` 이 `…` 로 잘리는데 바로 아래 부제에 같은 이름 반복 | 상태를 진행 중 / 완료 / 마감 지남 / 초안 / 보관 으로 나눠 기본은 진행 중 + 마감 지남. 상세 제목은 줄바꿈, 부제는 메타만 |

---

## 1. 선생님 계정 (`tester.t1@`, 선생님 역할)

### 1-1. 공통 셸 · 내비게이션

| P | 문제 | 제안 |
|---|---|---|
| P2 | 휴대폰 상단 탭이 가로 스크롤(447/375px) — 재시험 절반·SIGN OUT 안 보임, 스크롤 가능 표시 없음 | 하단 탭 바 또는 오른쪽 페이드 + SIGN OUT 을 ⇄ 메뉴로 |
| P2 | `/app/settings` · `/app/teachers` 를 선생님이 열면 **아무 안내 없이 `/app` 으로 튕김** | "학원장만 볼 수 있어요" 토스트, 또는 읽기 전용으로 보여주기 |
| P2 | 선생님에게 **내 계정 화면이 없음** — 이름 바꾸기·알림·연결된 로그인 수단·로그아웃이 흩어져 있음 | `/app/me`(또는 ⇄ 메뉴 안) 이름 · 로그인 수단 · 알림 · 로그아웃 |
| P3 | 정렬 헤더 `▲` 가 정렬 안 된 상태(aria-sort=none)에도 보여 "오름차순 정렬됨"으로 읽힘 (학생·성적·재시험 표 공통) | 미정렬은 `⇅` 흐리게, 정렬 중인 열만 ▲/▼ 진하게 |
| P3 | 휴대폰 24px 미만 터치 대상: 체크박스 13×13, 정렬 버튼 19px, `← STUDENTS` 14px, 히트맵 셀 14×14 링크(성적 125개) | 체크박스 20px + 여백, 뒤로가기·정렬은 세로 padding 8px, 히트맵 셀은 링크 대신 행 전체 링크 |

### 1-2. `/app` 오늘

| P | 문제 | 제안 |
|---|---|---|
| P2 | `TODAY 38/50 · 76% · 미응시 2` — 50−38=12 인데 "미응시 2"(기한 지남만 셈)라 한눈에 안 맞음 | "완료 38 / 배정 50 · 남음 12 (기한 지남 2)" |
| P2 | `DUE SOON 0/5` 의 의미 불명. 목록에 `D-5 · D-7` 과 `경과 37일 · 16일` 이 함께 | 지난 시험은 "기한 지남" 섹션으로 분리, 0/5 제거 |
| P2 | `RETAKE 14건 내기` vs `NEXT RETAKE DUE 출제 전 14명` — 학생은 5명뿐 | 단위 통일(건) |
| P3 | 카드 바닥 점 7개 = 요일별 채점 수 막대인데 전부 0 이라 장식/말줄임처럼 보임 | 값 없으면 숨기거나 `이번 주 채점 0` 라벨 |
| P3 | 머리글 1차 버튼이 `시험 탭 →` — 내비 문구지 행동 문구가 아님 | `+ 시험 만들기` (시험 탭의 버튼과 동일) |
| P3 | STUDENTS 모듈 `▼10 · = · ▲10` 범례 없음 | 라벨 `4주 평균 · 전주 대비` |
| P3 | `ASSIGNED TODAY 50` 에 8주 전 DONE 항목 포함 (시드 데이터 탓이지만 "오늘"이란 라벨과 안 맞음) | 오늘 배정 = `assignedAt` 기준으로 |

### 1-3. `/app/students` · `/app/students/[id]`

| P | 문제 | 제안 |
|---|---|---|
| **P1** | 반 칩 `테스트 B반 5` → 누르면 0명, `전체 0`. 빈 상태 문구가 "반 코드를 알려주거나 오른쪽에서 등록" | 칩 = 담당 학생 수, 담당 없는 반은 비활성. 빈 상태 문구는 담당 여부에 따라 |
| P2 | B반 필터를 골라도 INVITE 카드는 A반 코드 그대로 | 필터 반과 동기화 |
| P2 | 오른쪽 레일 카드 4개(INVITE · EXCEL · ONE · CLASSES) 세로로 김. 휴대폰은 표 아래 한참 내려가야 등록 | 휴대폰은 `학생 추가` 버튼 → 바텀시트(반 코드 / 엑셀 / 한 명) |
| P3 | 초대 코드 표기 `209 127`(카드) vs `209127`(칩) | 한 형식으로 (`209 127` 읽기 쉬움 → 칩도 동일) |
| P3 | 선생님도 CLASSES 카드에서 반을 **보관** 할 수 있음 | 담당 반만, 또는 학원장 전용 |
| P3 | 학생 상세 부제 `세종고 고1 · 담당 테스터 선생님1, 테스터 학원장, 김원장 · 계정 연결됨` 이 휴대폰에서 2줄 | 담당은 칩으로, 연결 상태는 배지로 |
| P3 | 학생 상세 반복 오답 목록 체크박스 13px | 위 터치 대상 항목과 함께 |

### 1-4. `/app/vocabulary` · `/app/vocabulary/[bookId]`

| P | 문제 | 제안 |
|---|---|---|
| P2 | 단어 목록 표 831px → 휴대폰 2.4배 가로 스크롤, 줄 작업(수정·출제 제외·삭제 ×23줄)이 화면 밖 | 휴대폰은 `영어 · 뜻` 두 줄 카드 + ⋯ 메뉴. 데스크톱도 줄 작업은 호버/⋯ 로 |
| P2 | 목록 카드 `DAY 7 · 시험 3` — "DAY 7" 이 7일차로 읽힘. 테스트 단어장은 실제 DAY 가 14~20 인데 `DAY 7` | `7일 · 시험 3` 또는 `DAY 14–20` |
| P2 | 이 단어장으로 만든 시험 요약에 `0/0` (대상 없는 발행 시험), 같은 이름 2개 | 0/0 → "대상 없음", 이름 차수 |
| P3 | `병합` 필이 버튼처럼 보이지만 체크박스(선택) — `열기 · 보관` 과 스타일이 달라 역할 불명 | 체크박스 모양 + "병합에 추가" 툴팁, 또는 목록 상단 `병합 모드` 토글 |
| P3 | 카드 제목이 링크가 아니고 작은 `열기`만 링크 | 제목·카드 전체 링크 |
| P3 | 카드의 막대 7개 그래픽에 라벨 없음 | `DAY 7 · 23·23·22…` 툴팁 또는 제거 |
| P3 | 열 이름 `유의어 (보기 제외)` 용어 | `유의어 · 보기에서 뺌` 또는 툴팁 |

### 1-5. 출제 — `/app/vocabulary/[bookId]/new-test` · `/app/tests/new`

| P | 문제 | 제안 |
|---|---|---|
| P2 | 위저드 1단계에 진행 표시가 `1 단어장` 뿐 — 전체 몇 단계인지 안 보임. 비활성 `다음 · 범위 →` 가 검정 바 위 탁한 갈색(대비 낮음) | 상단 `1/5 단어장 · 범위 · 대상 · 조건 · 이름` 스텝퍼, 비활성은 회색 |
| P3 | `전체 시간 제한 (분 · 0 = 없음)` 이 4 이름·공개 섹션에 있음 — 조건임 | 3 조건 섹션으로 이동 |
| P3 | 타이머 입력이 네이티브 스피너 `▲▼` — 필 UI 와 안 어울림 | `− 7 +` 스테퍼 또는 5·7·10 프리셋 |
| P3 | 미리보기 kicker `다이제보카 … —` 휴대폰에서 잘림 | 범위 없을 땐 `범위를 고르세요` |
| ok | 범위→대상→조건→미리보기→`초안만 저장 | 출제 · 5명에게 →` 흐름 자체는 명확 · 담당 학생 수 칩 · 정답 표시 미리보기 좋음 | — |

### 1-6. `/app/tests` · `/app/tests/[id]` · `/items` · `/targets` · `/scans`

| P | 문제 | 제안 |
|---|---|---|
| P2 | "진행 중" 기본 필터에 완료된 지난 시험 포함, 상태 열 전부 `발행` | 상태 세분화(위 #10) |
| P2 | 같은 이름 시험 2개, `0/0` · 마감 `-` | 위 #9 |
| P2 | 시험 표 1148px → 휴대폰 3.3배 스크롤 | 카드형 목록 (README P3 로도 언급됨) |
| P2 | 상세 `h1` 잘림(`…D…`) + 부제에 전체 이름 반복 | 제목 줄바꿈 |
| P2 | `문항 · 정답` 화면에 `seed 보호됨 · hash ffcff8fc65` — 개발 용어 노출 | 숨기거나 `문항 고정됨 (v1)` |
| P2 | 응시 대상 안내 "빠진 학생은 **오른쪽**에서 추가" — 좁은 폭에선 아래 | "아래/오른쪽" 대신 `대상 추가 카드에서` |
| P2 | `대상에서 제외` 강조색 줄 버튼이 휴대폰 스크롤 밖 | ⋯ 메뉴 또는 스와이프, 확인 다이얼로그 |
| P3 | 마감 열 `2026. 09. 30. 오후 10:32` 전체 표기 | `9/30 22:32` 또는 `D-7` |
| P3 | `사진 채점 →` 버튼이 필터 줄 아래 혼자 오른쪽에 떠 있음(좁은 폭) | 필터 줄 안으로 |
| P3 | PROGRESS 카드 `정답·오답노트 공개` 버튼에 현재 상태 표시 없음(이미 제출 직후 공개인지) | 토글 + 현재 값 `공개: 제출 직후` |
| P3 | `새 버전 초안 생성` 이 v1 칩보다 위 | v1 칩 아래 보조 버튼으로 |
| P3 | 사진 채점: 네이티브 파일 입력(단어장 드롭존과 다름), 빈 표가 업로드보다 위, `HEIC 는 JPG 로 변환` 요구(아이폰 기본 HEIC) | 드롭존 통일, 비어 있으면 업로드 먼저, 서버에서 HEIC 변환(`sharp`+libheif 또는 `heic-convert`) |

### 1-7. `/app/results`

| P | 문제 | 제안 |
|---|---|---|
| P2 | 모듈 8개 한 페이지(3,074/3,889px). 위젯 보드 오른쪽 90px 빈 공간, 2행 가운데 빈 칸, 같은 높이(396px) 때문에 `그룹별 평균`(막대 1개)이 거의 비어 보임 | 탭 분리(요약/학생별/시험별) · 보드 span 정리 · 카드 높이 내용에 맞춤 |
| P3 | 성적 추이 4주 창에 이번 주(데이터 없음) 포함 → 선이 9/14 에서 끊기고 오른쪽 1/4 비어 있음 | 이번 주는 점선/미확정 표시 또는 "지난 4주"로 |
| P3 | 히트맵 셀 14px 링크 125개 | 행 단위 링크 |
| P3 | 정렬 헤더 3개 모두 ▲ | 공통 항목 |

### 1-8. `/app/retakes`

| P | 문제 | 제안 |
|---|---|---|
| P2 | 14줄 × (세그먼트 + datetime + 강조 버튼 + 취소) → 휴대폰 3,567px, 강조 버튼 42개 | 학생별 접기, 한 줄 요약 + 펼침 |
| P2 | `취소` 의미·확인 없음 | `재시험 없이 종료` + 확인 |
| P2 | `완료 포함 전체` 가 혼자 떠 있는 텍스트 — 토글인지 링크인지, 현재 상태 불명 | 필터 칩 `출제 전 · 응시 대기 · 완료 포함` |
| P3 | 8주 전 미달까지 계속 큐에 남음 | N주 지나면 자동 종료 + "오래된 항목 3건 정리" |

---

## 2. 학생 계정 (`tester.s01@`)

> 서버가 중간에 멈춰 **아래는 코드 리뷰 기준**. 서버가 다시 뜨면 실제 응시까지 돌려 보고 갱신할 것. (러너 `TestRunner.tsx`, 홈 `learn/page.tsx`, 성적 `grades/page.tsx`, 재시험 `retake/page.tsx`, 레이아웃 `learn/layout.tsx`)

| P | 어디 | 문제 | 제안 |
|---|---|---|---|
| **P1** | 러너 | `응시 시작` → 러너 로드 즉시 단어별 7초 카운트다운. 준비 화면·3-2-1 없음. 보기를 누르면 220ms 뒤 바로 다음 단어(되돌리기 없음) | 준비 화면(문항 수 · 초/단어 · 규칙) + [시작] · 첫 단어 전 3-2-1 · 잘못 누름 방지(같은 단어에서 300ms 내 재선택 허용 또는 "확정" 없이 자동 넘김을 옵션으로) |
| P2 | 러너 | 시간 초과가 무응답으로 넘어가지만 학생에게 "놓쳤어요" 피드백이 짧음(`timeouts` 카운트만) | 넘어갈 때 0.3초 빨간 플래시 + "시간 초과" |
| P2 | 홈·전체 | 영어 마이크로 카피가 내용 문구까지 침범: `20 Q · Pass 90 · 7s / word`, `no due`, `due 09.27`, `OPEN/WAIT/SOON`, `TODO/DONE`, `Best · Retakes · Practice · RANDOM →`, `PASS / RETAKE`, `mine`, 휴대폰 로그아웃 버튼 `out` | 킥커(라벨)는 영어 유지해도 되지만 **값·상태·버튼은 한국어**: `20문항 · 통과 90점 · 단어당 7초`, `마감 없음`, `9/27까지`, `응시 가능 / 대기 / 준비 중`, `할 일 1 · 완료 0`, `최고점 · 재시험 · 연습`, `통과 / 재시험`, `내 답`, `로그아웃` |
| P2 | 홈 | 다음 시험 카드 `D-7` 큰 숫자 + 날짜 — 예약 시험은 "… 부터 응시할 수 있어요" 문장만 있고 버튼 없음(눌러볼 게 없어 멈춘 듯 보임) | 비활성 버튼 `9/24 09:00 시작` |
| P2 | 홈 | 학원 미연결 시 `학원과 연결하기` 는 좋으나, 연결 대기(pending) 문구가 카드 하나뿐이고 새로고침 안내 없음 | `확인 중 · 새로고침` 버튼 + 알림 |
| P2 | 성적 `History` | 미달 배지가 `RETAKE` — "재시험이 잡혔다"로 읽힘(아직 출제 전일 수 있음) | `미달` / `통과`, 재시험이 실제 잡히면 `재시험 →` 링크 |
| P3 | 성적 | 12주 스파크라인 라벨 9px(`.lbl` 9px) — 휴대폰에서 읽기 어려움 | 최소 10.5px |
| P3 | 재시험 | 지난 재시험 배지 `PASSED · 90` / `RETRY` | `통과 90` / `다시` |
| P3 | 결과 | 오답노트 PDF `target=_blank` 링크가 버튼 모양 — 새 탭 열림 표시 없음 | `PDF ↗` |
| P3 | 종이 제출 | 상태 배지 6종(채점 대기·채점 중·확인 중·확정·인식 실패·실패) — 학생에겐 "채점 중 / 완료 / 다시 찍어 주세요" 3단계면 충분 | 3단계로 묶고 실패엔 재촬영 버튼 |
| P3 | 탭 | 휴대폰 세그먼트 탭 2줄(영문 + 한글) — 높이는 좋지만 정보 중복 | 한글만 크게 |

---

## 3. 다음 QA 에서 확인할 것

- `/login` 500 수정 후: 로그인 상태로 `/login`·`/login/email`·`/welcome` 재방문, 자리 1개/2개 계정 모두.
- 휴대폰 실제 기기(iOS Safari · Android Chrome)에서 러너 진동·키보드·뒤로가기 동작, 하단 탭 바 도입 시 safe-area.
- `pnpm run qa:visual` 에 위 표 6곳의 "카드형 목록" 전환 후 가로 넘침 0 재확인.
- 시각 QA 도구 한계: 내장 브라우저 패널의 뷰포트 에뮬레이션 스크린샷이 불안정해 데스크톱/휴대폰 시각 확인은 DOM 검사로 대체함 — 다음엔 `qa:visual` 스크린샷(`log/qa-visual/`)을 같이 보는 게 빠름.
